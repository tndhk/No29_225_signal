# ローカルキャッシング機能ガイド

## 概要

バックテストと日次分析で使用するYahoo Financeのデータをローカルにキャッシュし、API呼び出しを削減します。

**効果:**
- ✅ バックテスト初回実行: ~73秒 → 2回目以降: ~27秒
- ✅ 単一銘柄の取得速度: API 0.60秒 → キャッシュ 0.01秒 (**45倍高速化**)
- ✅ API呼び出し削減によるレート制限回避

## キャッシュの仕組み

### キャッシュディレクトリ
```
data_cache/
├── 1332.T_1y.csv      # 1年分データ
├── 1332.T_2y.csv      # 2年分データ
├── 1605.T_1y.csv
└── ...
```

各ファイルは `{ticker}_{period}.csv` の形式で保存されます。

### キャッシュの動作フロー

```
fetch_daily_data(ticker, period="1y")
  ├─ キャッシュが存在？ YES
  │   └─→ キャッシュから読み込み (0.01秒)
  │
  └─ キャッシュなし
      ├─→ APIから取得 (0.60秒)
      ├─→ キャッシュに保存
      └─→ データを返す
```

## 使用方法

### 1. バックテスト実行

#### 初回実行（APIからデータを取得・キャッシュに保存）
```bash
python -m src.backtest
```
初回実行時は約73秒かかり、すべてのデータがキャッシュに保存されます。

#### 2回目以降（キャッシュから高速実行）
```bash
python -m src.backtest
```
キャッシュが存在する場合、約27秒で実行完了します。

#### キャッシュを強制リフレッシュ
```bash
python -m src.backtest --refresh
```
`--refresh` フラグで古いキャッシュを無視し、最新データをAPIから再取得します。

### 2. 日次分析実行

#### キャッシュを使用（推奨）
```bash
python -m src.main
```
キャッシュが存在すれば、高速に実行されます。

#### キャッシュを強制リフレッシュ（毎日実行時）
```bash
python -m src.main --refresh
```
毎日最新データを取得する場合は、このオプションを使用してください。

### 3. テスト実行
```bash
python test_cache.py
```
キャッシング機能の動作確認とパフォーマンス測定ができます。

出力例:
```
=== Cache Test for 7203.T ===
Period: 1y

1. First fetch (from API, will be cached)...
   - Time taken: 0.60s
   - Records fetched: 245

2. Second fetch (from cache)...
   - Time taken: 0.01s
   - Records fetched: 245

4. Performance improvement:
   - API fetch: 0.60s
   - Cache fetch: 0.01s
   - Speedup: 45.5x faster
```

## キャッシュの管理

### キャッシュサイズ確認
```bash
du -sh data_cache/
```

### キャッシュの削除（全削除）
```bash
rm -rf data_cache/
```

初期化後、次回実行時にすべてのデータが再度キャッシュされます。

### 特定銘柄のみ削除
```bash
rm data_cache/7203.T_*.csv
```

## 実装の詳細

### [data_loader.py](src/data_loader.py)

新規関数:
- `_get_cache_path()`: キャッシュファイルパスの取得
- `_load_from_cache()`: キャッシュからのデータ読み込み
- `_save_to_cache()`: キャッシュへのデータ保存

修正された関数:
- `fetch_daily_data()`: パラメータ追加
  - `use_cache=True`: キャッシュの使用を有効化
  - `refresh=False`: キャッシュの強制リフレッシュ

### [backtest.py](src/backtest.py)

修正内容:
- `run_backtest()` に `refresh` パラメータを追加
- コマンドライン引数 `--refresh` をサポート

### [main.py](src/main.py)

修正内容:
- `main()` に `refresh` パラメータを追加
- コマンドライン引数 `--refresh` をサポート

## 注意事項

### キャッシュの有効期限

キャッシュに有効期限は設定されていません。定期的に（例：月1回）以下を実行して最新データを取得してください：

```bash
python -m src.backtest --refresh
```

### APIレート制限

キャッシングにより、Yahoo FinanceのAPIレート制限（通常は1分間に2000リクエスト）に引っかかる可能性がほぼ0になります。

### キャッシュの一貫性

複数の期間（例：1y, 2y）でデータを取得した場合、それぞれ独立してキャッシュされます：
- `1332.T_1y.csv`: 1年分データ
- `1332.T_2y.csv`: 2年分データ

必要に応じて両方をリフレッシュしてください。

## パフォーマンス比較

| 実行パターン | 時間 | 特徴 |
|-----------|-----|------|
| API直接（全新規） | 73秒 | 遅い、APIレート制限のリスク |
| キャッシュ利用 | 27秒 | **高速、推奨** |
| キャッシュリフレッシュ | 73秒 | 最新データ取得時のみ |

## トラブルシューティング

### キャッシュが更新されない
→ `--refresh` フラグを使用して強制リフレッシュしてください。

### キャッシュファイルが破損している
→ 該当ファイルを削除し、再度実行してください：
```bash
rm data_cache/TICKER_SYMBOL_*.csv
```

### メモリ不足で実行が遅い
→ キャッシュをクリアして再実行してください：
```bash
rm -rf data_cache/
python -m src.backtest
```

## 今後の改善案

1. **キャッシュの有効期限管理**: タイムスタンプを記録し、古いキャッシュを自動削除
2. **インクリメンタル更新**: 最新日付以降のデータのみ追加取得
3. **キャッシュ統計**: キャッシュヒット率、容量などの統計情報表示
4. **キャッシュ圧縮**: gz形式での圧縮保存でディスク使用量削減
